<UserControl x:Class="GamingCenter.App.Views.Parts.ToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource Bg.Sidebar}" BorderBrush="{DynamicResource Border.Strong}" BorderThickness="1" CornerRadius="10" Padding="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="8" Opacity="0.4" Direction="270"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="10,0,0,10" Background="{Binding Kind, Converter={StaticResource StatusBrush}, ConverterParameter=Toast.}"/>
            <StackPanel Grid.Column="1" Margin="14,12,8,12">
                <TextBlock Text="{Binding Title}" Style="{StaticResource T.Base}" FontWeight="SemiBold" TextWrapping="Wrap" TextTrimming="None"/>
                <TextBlock Text="{Binding Message}" Style="{StaticResource T.Muted}" TextWrapping="Wrap" TextTrimming="None" Margin="0,3,0,0"
                           Visibility="{Binding Message, Converter={StaticResource HasValueToVis}}"/>
            </StackPanel>
            <Button Grid.Column="2" Style="{StaticResource Btn.Link}" Margin="0,10,12,0" VerticalAlignment="Top" Click="OnDismiss" ToolTip="Dismiss">
                <TextBlock Text="×" FontSize="16" Foreground="{DynamicResource Text.Faint}"/>
            </Button>
        </Grid>
    </Border>
</UserControl>
